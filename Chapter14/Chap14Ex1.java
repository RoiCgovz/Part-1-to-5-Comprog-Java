
public class Chap14Ex1 
{
    public static void main (String[] args)
    {
        double angle = -90.0;

        double angleInRadians = Math.toRadians(angle);
        double sineValue = Math.sin(angleInRadians);
        System.out.print("angle       sine");
        System.out.print("\n----     ---------");
        System.out.printf("\n%.1f      %.1f", angle, sineValue);

        angle += 15.0;
        
        angleInRadians = Math.toRadians(angle);
        sineValue = Math.sin(angleInRadians);
        System.out.printf("\n%.1f      %.6f", angle, sineValue);

        angle += 15.0;
        
        angleInRadians = Math.toRadians(angle);
        sineValue = Math.sin(angleInRadians);
        System.out.printf("\n%.1f      %.6f", angle, sineValue);

        angle += 15.0;
        
        angleInRadians = Math.toRadians(angle);
        sineValue = Math.sin(angleInRadians);
        System.out.printf("\n%.1f      %.6f", angle, sineValue);

        angle += 15.0;
        
        angleInRadians = Math.toRadians(angle);
        sineValue = Math.sin(angleInRadians);
        System.out.printf("\n%.1f      %.6f", angle, sineValue);

        angle += 15.0;
        
        angleInRadians = Math.toRadians(angle);
        sineValue = Math.sin(angleInRadians);
        System.out.printf("\n%.1f      %.6f", angle, sineValue);

        angle += 15.0;
        
        angleInRadians = Math.toRadians(angle);
        sineValue = Math.sin(angleInRadians);
        System.out.printf("\n  %.1f       %.1f", angle, sineValue);

        angle += 15.0;
        
        angleInRadians = Math.toRadians(angle);
        sineValue = Math.sin(angleInRadians);
        System.out.printf("\n %.1f       %.6f", angle, sineValue);

        angle += 15.0;
        
        angleInRadians = Math.toRadians(angle);
        sineValue = Math.sin(angleInRadians);
        System.out.printf("\n %.1f       %.6f", angle, sineValue);

        angle += 15.0;
        
        angleInRadians = Math.toRadians(angle);
        sineValue = Math.sin(angleInRadians);
        System.out.printf("\n %.1f       %.6f", angle, sineValue);

        angle += 15.0;
        
        angleInRadians = Math.toRadians(angle);
        sineValue = Math.sin(angleInRadians);
        System.out.printf("\n %.1f       %.6f", angle, sineValue);

        angle += 15.0;
        
        angleInRadians = Math.toRadians(angle);
        sineValue = Math.sin(angleInRadians);
        System.out.printf("\n %.1f       %.6f", angle, sineValue);

        angle += 15.0;
        
        angleInRadians = Math.toRadians(angle);
        sineValue = Math.sin(angleInRadians);
        System.out.printf("\n %.1f       %.1f", angle, sineValue);
    }
}
